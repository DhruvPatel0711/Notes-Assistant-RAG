"""
LCEL RAG chain — end-to-end question → answer pipeline.

Uses RunnableParallel for single-retrieval, context formatting,
prompt injection, LLM call, and output parsing.
Implemented in Phase 5.
"""

# TODO: Phase 5 — implement rag_chain, ask(), format_docs(), extract_sources()
